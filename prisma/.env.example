# Database Connection
# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL="postgresql://postgres:password@localhost:5432/donations?schema=public"

# Production Example (AWS RDS):
# DATABASE_URL="postgresql://admin:SecurePassword123@donations-db.cluster-xxx.us-east-1.rds.amazonaws.com:5432/donations?schema=public&connection_limit=10&pool_timeout=20"

# Development (local PostgreSQL):
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/donations_dev?schema=public"

# Testing (separate test database):
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/donations_test?schema=public"

# Connection Pool Settings (for production)
# Add these query parameters to DATABASE_URL:
# - connection_limit=10 (max connections per instance)
# - pool_timeout=20 (seconds to wait for connection)
# - connect_timeout=10 (seconds to wait for initial connection)
